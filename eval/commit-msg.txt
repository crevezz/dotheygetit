The card test was reading a hidden card

Every content assertion in eval/card-eval.js passed while the card was invisible.
The test drew straight into #results, which lives inside #classPanel - and that
panel stays hidden until a teacher clicks into a class. innerText falls back to
textContent on a non-rendered element, so "the evidence is on the card" and "the
card is 0x0 with no offsetParent" were both true at once.

It now opens the class and the check through the teacher's own clicks, then
measures the layout instead of trusting it: the evidence block has real height
(711x203), every line is at least 11px, and nothing overflows or is clipped. The
preview is written to eval/card-preview.png for a person to look at, because no
assertion in here can tell you whether it reads well.

23 checks, 0 failures - including clicking "you decide" and finding the change in
the store with the marking's own answer kept beside it.
